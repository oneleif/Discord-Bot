{
  "name": "@oneleif/discord-bot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "start": "nodemon -e ts -x \"cross-env rm -rf dist && tsc && node .\"",
    "build": "cross-env rm -rf dist && tsc",
    "lint": "prettier --write src/*"
  },
  "devDependencies": {
    "@types/chalk": "^2.2.0",
    "@types/dotenv": "^8.2.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.22.0",
    "nodemon": "^2.0.19",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@octokit/core": "^4.0.5",
    "chalk": "^5.0.1",
    "discord.js": "^14.2.0",
    "dotenv": "^16.0.1"
  }
}
